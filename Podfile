# Minimum iOS version (required for Combine framework)
platform :ios, '13.0'

target 'ios-app' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ios-app
  pod 'Moya'
  pod 'SnapKit'
  pod 'KeychainSwift'
  pod 'IQKeyboardManagerSwift'

  target 'ios-appTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ios-appUITests' do
    # Pods for testing
  end

end
